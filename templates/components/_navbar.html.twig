<nav class="navbar navbar-expand-lg bg-primary">
	<div class="container-fluid">
		<h1 class="logo navbar-brand text-white fs-1 ms-3 me-5" href="#">Playfree</h1>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active text-white ms-4" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link text-white ms-4" href="{{ path('catalogue_index') }}">Catalogue</a>
				</li>
				{% if app.user %}
				<li class="nav-item">
					<a class="nav-link text-white ms-4" href="{{ path('catalogue_voirbookings') }}">Réservations</a>
				</li>
				<li class="nav-item">
					<a class="nav-link text-white ms-4" href="{{ path('app_logout') }}">Déconnexion</a>
				</li>
				{% else %}
				<li class="nav-item">
					<a class="nav-link text-white ms-4" href="{{ path('register') }}">S'inscrire</a>
				</li>
				<li class="nav-item">
					<a class="nav-link text-white ms-4" href="{{ path('app_login') }}">Connexion</a>
				</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
