{% extends 'base.html.twig' %}

{% block title %}Inscription
{% endblock %}

{% block stylesheets %}
	{{ encore_entry_link_tags('catalogue') }}
{% endblock %}

{% block body %}
	<section class="container-fluid mt-5">
		<div class="row gap-5 justify-content-center">
			<div class="col-10 col-md-4 col-lg-3 card card-show bg-dark">
				<h3 class="game-title text-center mt-4 mb-4 pt-3 fs-2">Mon jeu du moment :</h3>
				<h2 class="reserved text-center fs-3">{{reservation.game.title}}</h2>
				<img class="img-responsive rounded" src="{{reservation.game.image}}" alt="Image du jeu">
				<p class="mt-3">Date de début :
					{{reservation.begin|date('d-m-Y')}}</p>
				<p class="mt-3">Date de fin :
					{{reservation.end|date('d-m-Y')}}</p>
			</div>
		</div>
	</div>
</div></section>{% endblock %}
