{% extends 'base.html.twig' %}

{% block stylesheets %}
	{{ encore_entry_link_tags('catalogue') }}
{% endblock %}

{% block body %}
	<section class="container-fluid">
		<div class="row gap-5 justify-content-center">
			<div class="col-sm-6 col-md-4 col-lg-3 card card-show bg-dark">
				<h3 class="hometitle text-center mt-4 mb-4 pt-3 fs-4">{{game.title}}</h3>
				<img class="img-responsive rounded" src="{{ game.image }}" alt="Image du jeu">
				<div class="d-flex flex-column justify-content-around">
					<p>Genre : {{game.genre}}</p>
					<p>Sortie :</p>
					<p>Réserver ?</p>
				</div>
			</div>
		</div>
	</div>
</section>{% endblock %}
